elementary(htmlwrap(geodesy({
    geodesy: {
        origin: "[0,0]",
        motif: "pyritohedron",
        bitmask: "8 ",
        radius: "100px" // the px actually gets ignored but its px
    }
})))